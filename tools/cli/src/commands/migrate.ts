// @enbi/cli — `enbi migrate` placeholder (filled by the migrations sub-project).
import { EnbiError } from "@enbi/db";

export function runMigrate(): never {
  throw new EnbiError("config", "enbi migrate — coming soon (migrations sub-project).");
}
